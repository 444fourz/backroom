# Local Postgres (see docker-compose.yml)
DATABASE_URL="postgresql://backroom:backroom@localhost:5432/backroom"

# NextAuth
# Generate with: npx auth secret   (or: openssl rand -base64 32)
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Dev-only password shared by all seeded users (see prisma/seed.ts)
SEED_USER_PASSWORD="backroom-dev-2026"
